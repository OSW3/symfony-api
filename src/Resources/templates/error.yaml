success: "{status.is_success}"
status: "{status.code}"
message: "{status.text,ddd}"
errors: "{response.data}"
meta:
    request_id: "{meta.operation_id}"
    version: "{app.version}"
    timestamp: "{response.timestamp}"